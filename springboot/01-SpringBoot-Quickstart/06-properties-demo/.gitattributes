/mvnw text eol=lf
*.cmd text eol=crlf

# Standardmäßige automatische Textdateierkennung durch Git
* text=auto

# Java-Quellcode als Text behandeln
*.java text

# XML- und Properties-Dateien
*.xml text
*.properties text

# Shell-Skripte mit Unix-Zeilenenden
*.sh text eol=lf

# Windows-Batchdateien mit Windows-Zeilenenden
*.bat text eol=crlf

# Markdown-Dateien (Readmes, Doku)
*.md text

# JSON- und YAML-Dateien
*.json text
*.yml text
*.yaml text

# Bilddateien als binär markieren (nicht diffbar)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary

# PDF und andere binäre Formate
*.pdf binary

# IntelliJ-Konfigurationsdateien (optional, falls nicht ignoriert)
*.iml text
