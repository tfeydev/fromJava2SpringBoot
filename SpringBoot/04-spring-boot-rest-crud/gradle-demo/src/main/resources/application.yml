spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/dbtechthor
    username: techthor
    password: changeme
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    rest:
      base-path: /api
